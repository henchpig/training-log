// Firebase web config. These values are NOT secret (they identify your project,
// they don't authorize access) — it's normal and safe to commit this file.
// All real access control happens in firestore.rules.
//
// Get these from: Firebase Console → Project settings → General → Your apps → Web app → SDK setup
window.FIREBASE_CONFIG = {
  apiKey: "REPLACE_ME",
  authDomain: "REPLACE_ME.firebaseapp.com",
  projectId: "REPLACE_ME",
  storageBucket: "REPLACE_ME.appspot.com",
  messagingSenderId: "REPLACE_ME",
  appId: "REPLACE_ME"
};
