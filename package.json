{
  "name": "training-log",
  "version": "1.0.0",
  "private": true,
  "description": "Climbing-focused training logger — static frontend, Firebase backend",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node test/ui.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
